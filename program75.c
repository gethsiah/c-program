#include<stdio.h>
int main()
char*strmid(char *texte, int d, int longr)
{ char *result = " ";
char *temp = " ";
int nbr,n;
nbr=d+longr - 1;
strncat(temp,texte,nbr);
strrev(temp);
strncat(result,temp,longr+1);
strrev(result);
return result;
printf("enter the element name");
scanf("%d",&n);
(if=(mid=*))
printf("the mid element can change after %d",n);

}
