#include <stdio.h>
#define MAX_SIZE 2000
int main()
{
int arr[MAX_SIZE];
int n, N;
printf("Enter the size of array\n%d",N);
printf("%dEnter elements in the array\n%d", N,arr[n]);
for(n=0; n<N; n++)
printf("\nElements in array are");
for(n=0; n<N; n++)
{
printf("%d, ", arr[n]);
}
return 0;
}
