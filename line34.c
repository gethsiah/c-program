#include<stdio.h>
#include <string.h>
int main()
{
char s[50];
int i, line;
printf("enter the string\n");
scanf("%s", s);
for(i=0 ; s[0]!="\0" ; i++
{
if (b[i]='    ')
{
line++;
}
}
printf("Number of spaces is %d", line);
return 0;
}
